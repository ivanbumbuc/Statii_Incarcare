@model IEnumerable<Statii_Incarcare.Models.Db.Statii>

@{
    ViewData["Title"] = "Statii";
    Layout = "_Layout";
}

<h1>Statii Electrice</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Oras)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Adresa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Oras)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adresa)
            </td>

            <td>
                <a asp-action="Edit" asp-route-id="@item.StatieId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StatieId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StatieId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
